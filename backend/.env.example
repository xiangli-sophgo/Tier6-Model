# LLM Simulator Backend 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# CORS 允许的来源（逗号分隔）
# 开发环境默认配置（支持 localhost 和 127.0.0.1）
ALLOWED_ORIGINS=http://localhost:3100,http://localhost:3000,http://127.0.0.1:3100,http://127.0.0.1:3000

# 生产环境示例：
# ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com

# 数据库连接 URL
# DATABASE_URL=sqlite:///./tier6_model.db

# 最大并发 worker 数量
# MAX_GLOBAL_WORKERS=4
