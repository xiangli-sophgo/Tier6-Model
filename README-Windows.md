# Tier6+ Windows 快速启动指南

## 当前环境

✅ Python 3.14.2 已安装 (命令: `python3`)
✅ Node.js v24.13.0 已安装
✅ pnpm 10.28.1 已安装

## 🚀 快速启动 (推荐)

### 方式 1: 双击运行
双击 `quick-start.bat` 即可启动

### 方式 2: VS Code 任务
1. 按 `Ctrl+Shift+P`
2. 输入 "Tasks: Run Task"
3. 选择 **"启动 Tier6+ 工具"**

### 方式 3: 命令行
```cmd
quick-start.bat
```

## 📦 首次使用

### 安装依赖

**自动安装 (推荐)**:
```cmd
setup.bat
```

**手动安装**:
```cmd
# 后端依赖
cd backend
python3 -m pip install -r requirements.txt

# 前端依赖
cd frontend
pnpm install
```

## 🛠️ VS Code 可用任务

在 VS Code 中按 `Ctrl+Shift+P`，然后选择 "Tasks: Run Task"，可以看到：

- **启动 Tier6+ 工具** - 同时启动前后端
- **启动后端服务** - 仅后端 (8001端口)
- **启动前端服务** - 仅前端 (3100端口)
- **安装所有依赖** - 自动安装前后端依赖
- **安装后端依赖** - 仅后端
- **安装前端依赖** - 仅前端
- **构建前端** - 生产构建
- **运行后端测试** - pytest

## 🌐 访问地址

启动成功后访问:
- **前端界面**: http://localhost:3100
- **后端 API**: http://localhost:8001
- **API 文档**: http://localhost:8001/docs

## ⚠️ 常见问题

### 1. Python 命令问题
你的系统使用 `python3` 而不是 `python`，所有脚本已配置正确。

### 2. 端口被占用
如果端口冲突，手动指定端口：
```cmd
# 后端使用 8002
python3 -m uvicorn llm_simulator.api:app --port 8002 --reload

# 前端会自动选择其他端口
pnpm run dev
```

### 3. 依赖安装慢
使用国内镜像加速：
```cmd
# pip 使用清华镜像
python3 -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# pnpm 使用淘宝镜像
pnpm config set registry https://registry.npmmirror.com
pnpm install
```

### 4. 停止服务
- 关闭对应的终端窗口
- 或在终端中按 `Ctrl+C`

## 📁 项目结构

```
Tier6-Model/
├── quick-start.bat      # 快速启动脚本 ⭐
├── setup.bat            # 依赖安装脚本
├── start.bat            # 标准启动脚本
├── .vscode/
│   └── tasks.json       # VS Code 任务配置
├── backend/
│   ├── llm_simulator/   # 模拟器核心代码
│   └── requirements.txt # Python 依赖
└── frontend/
    ├── src/             # React 源代码
    └── package.json     # Node 依赖
```

## 🎯 下一步

1. 运行 `quick-start.bat` 启动应用
2. 浏览器会自动打开 http://localhost:3100
3. 在界面中配置拓扑、模型、硬件参数
4. 点击"运行模拟"查看结果

## 📚 更多文档

- `INSTALL.md` - 详细安装指南
- `CLAUDE.md` - 项目架构说明
- `README.md` - 项目总体介绍
