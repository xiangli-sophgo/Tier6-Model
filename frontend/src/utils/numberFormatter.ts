/**
 * @deprecated 此文件已废弃，请直接使用 ./formatters.ts
 *
 * 所有格式化函数已迁移至 utils/formatters.ts
 * 请更新导入语句：
 *   from './utils/numberFormatter' -> from './utils/formatters'
 */

export {
  formatNumber,
  formatMetric,
  formatPercent,
  formatLargeNumber,
} from './formatters'
